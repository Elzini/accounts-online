# 🖥️ دليل بناء ملف EXE لنظام Windows

هذا الدليل يشرح كيفية بناء تطبيق سطح المكتب (ملف EXE) من المشروع.

## 📋 المتطلبات

قبل البدء، تأكد من توفر:
- **Node.js** (الإصدار 18 أو أحدث) - [تحميل](https://nodejs.org/)
- **Git** - [تحميل](https://git-scm.com/)
- **نظام Windows** (لبناء ملفات EXE)

## 🚀 خطوات البناء

### 1️⃣ نقل المشروع إلى GitHub

1. اضغط على زر **"Export to GitHub"** في Lovable
2. اختر اسم المستودع وأكمل العملية

### 2️⃣ استنساخ المشروع محلياً

```bash
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
cd YOUR_REPO
```

### 3️⃣ تثبيت التبعيات

```bash
npm install
```

### 4️⃣ إضافة سكربتات البناء

افتح ملف `package.json` وأضف هذه السكربتات في قسم `"scripts"`:

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "electron:dev": "concurrently \"vite\" \"wait-on http://localhost:5173 && electron electron/main.js --dev\"",
    "electron:build": "npm run build && electron-builder",
    "electron:build:win": "npm run build && electron-builder --win",
    "electron:build:portable": "npm run build && electron-builder --win portable"
  }
}
```

أضف أيضاً في المستوى الرئيسي للملف:
```json
{
  "main": "electron/main.js",
  "author": "Your Name",
  "description": "نظام إدارة معرض السيارات"
}
```

### 5️⃣ تشغيل في وضع التطوير (اختياري)

```bash
npm run electron:dev
```

### 6️⃣ بناء ملف EXE

**للبناء مع مُثبِّت (Installer):**
```bash
npm run electron:build:win
```

**للبناء Portable (بدون تثبيت):**
```bash
npm run electron:build:portable
```

### 7️⃣ موقع الملفات

ستجد الملفات في مجلد `release/`:
- `نظام إدارة معرض السيارات-1.0.0-x64.exe` - مُثبِّت للنظام
- `نظام إدارة معرض السيارات-Portable-1.0.0.exe` - نسخة محمولة

## 🔧 إعدادات إضافية

### تغيير أيقونة التطبيق

1. أنشئ أيقونة بصيغة `.ico` بحجم 256x256 بكسل
2. ضعها في مجلد `public/` باسم `icon.ico`
3. حدّث ملف `electron-builder.json`:
   ```json
   "win": {
     "icon": "public/icon.ico"
   }
   ```

### تغيير اسم التطبيق

حدّث في ملف `electron-builder.json`:
```json
{
  "productName": "اسم التطبيق الجديد"
}
```

## ⚠️ ملاحظات مهمة

1. **قاعدة البيانات**: التطبيق يتصل بقاعدة البيانات السحابية (Cloud) - لا يحتاج قاعدة بيانات محلية
2. **الإنترنت**: يتطلب اتصال إنترنت للعمل
3. **التحديثات**: أي تغييرات في قاعدة البيانات تظهر تلقائياً
4. **الأمان**: جميع البيانات مشفرة ومحمية

## 🆘 حل المشاكل

### خطأ في البناء
```bash
npm cache clean --force
rm -rf node_modules
npm install
```

### خطأ في Electron
```bash
npm rebuild
```

### ملف EXE لا يعمل
تأكد من:
- وجود اتصال إنترنت
- عدم حجب الجدار الناري للتطبيق

## 📞 الدعم

للمساعدة، تواصل مع فريق الدعم أو افتح issue في GitHub.
